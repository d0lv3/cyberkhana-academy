# API base (default works for local dev)
VITE_API_URL=http://localhost:5002/api

# Google OAuth — paste the same Web client ID as backend/.env GOOGLE_CLIENT_ID.
# When set, the login page switches from the dev session to real Google sign-in.
VITE_GOOGLE_CLIENT_ID=
